---
import LoginPageLayout from "@/layouts/LoginPageLayout.astro";
import LoginForm from "@/components/login/LoginForm.vue";

const loginMode = import.meta.env.LOGIN_MODE;
const loginProviders = import.meta.env.OAUTH_PROVIDERS
  ? import.meta.env.OAUTH_PROVIDERS.split(",")
  : [];
---

<LoginPageLayout
  title="Login · Reliably"
  description="Log in to your Reliably account to store and run your chaos engineering experiments, and measure their impact on your Service Level Objectives."
>
  <LoginForm client:load mode={loginMode} providers={loginProviders} />
</LoginPageLayout>
